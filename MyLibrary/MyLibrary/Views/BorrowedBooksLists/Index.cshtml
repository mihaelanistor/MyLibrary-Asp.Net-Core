@model IEnumerable<MyLibrary.Models.BorrowedBooksList>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookId)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookId)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.BorrowedBooksListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BorrowedBooksListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BorrowedBooksListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
